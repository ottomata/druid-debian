[Unit]
Description=Druid Broker Node

[Service]
User=druid
Group=druid

# Load any environment overrides from this file.
EnvironmentFile=-/etc/default/druid-broker

ExecStart=/usr/bin/druid server broker

[Install]
WantedBy=multi-user.target
